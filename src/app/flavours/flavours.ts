import { Component } from '@angular/core';

@Component({
  selector: 'jelly-flavours',
  imports: [],
  templateUrl: './flavours.html',
  styleUrl: './flavours.scss',
})
export class JellyFlavoursComponent {
  //TODO: add methods onClick so each color will trigger a class on the application. Only one class can be triggered at a time.
}
